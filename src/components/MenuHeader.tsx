import React from 'react'

/**
 * Top slice of the menu screen — fills 3fr row (16:3)
 */
export default function MenuHeader() {
  return (
    <div
      style={{
        width: '100%',
        height: '100%',
        background: '#00e5ff', // cyan
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        padding: '1rem 2rem',
        boxSizing: 'border-box',
      }}
    >
      {/* Menu header content will go here later */}
    </div>
  )
}
